/**
 * Daily tasks HTTP surface lives in `server/modules/tasks/`.
 * Export name `dailyTasksRouter` preserved for mounts.
 */
export { tasksRouter as dailyTasksRouter } from "../modules/tasks/index.js";
