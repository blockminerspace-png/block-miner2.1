# Dependencies
node_modules/
client/node_modules/
server/node_modules/

# Env / secrets
# Guia local de deploy (pode conter notas sensíveis) — não commitar
deploy.md
# Ficheiro único de deploy (host/user/password) — ler deploy.secrets.example
deploy.secrets.local
# Credenciais + wrapper de deploy na raiz (nunca commitar)
deploy.py
scripts/vm_config_secret.py
scripts/deploy_credentials.local.py
.deploy-pw.txt
deploy-credentials.local.md
.env
.env.local
.env.*.local
env_new.tmp
.env.production
.env.production.vm-backup
!.env.example

# SQLite / local data
data/*.db
data/*.db-journal
data/*.sqlite
data/*.sqlite3
data/*.db-shm
data/*.db-wal
data/*.sqlite-shm
data/*.sqlite-wal

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS / Editor
.DS_Store
Thumbs.db
.vscode/

# Certs / backups (never commit real TLS keys; pasta existe via .gitkeep)
nginx/certs/*.pem
nginx/certs/*.pfx
nginx/certs/*.key
backups/

/src/generated/prisma
