# Scripts arquivados

Scripts de diagnóstico ou manutenção pontual **sem** entrada em `package.json` nem no fluxo de deploy atual.

Para usar, copiar de volta para `scripts/` ou executar com caminho explícito após revisão de segurança (sem segredos hardcoded).
