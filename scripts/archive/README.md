# Scripts arquivados

Scripts movidos de `scripts/` por estarem marcados como `@deprecated` ou substituídos por fluxos mais recentes.

| Script | Motivo |
|--------|--------|
| `replay-tournament-actions.mjs` | Unsafe — use `migrate-offerwall-run.mjs project` |
| `backfill-tournament-actions.mjs` | Substituído por `migrate-offerwall-run.mjs backfill` |
